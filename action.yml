name: "sil-lift validate"
description: >-
  Validate a LIFT (Lexicon Interchange Format) lexicon file with sil-lift --
  no Python toolchain required.
inputs:
  path:
    description: "Path to the .lift file to validate, relative to the workspace."
    required: true
  format:
    description: "Output format: text or json."
    required: false
    default: "text"
  strict:
    description: "Treat warnings as errors (exit 1 if any are found)."
    required: false
    default: "false"
  no-check-media:
    description: "Skip the filesystem media-presence check."
    required: false
    default: "false"
  allow:
    description: >-
      Problem codes to report but leave out of the pass/fail decision,
      separated by commas, spaces, or newlines (e.g. "uri-not-rfc schema").
    required: false
    default: ""
runs:
  using: "docker"
  image: "Dockerfile"
  entrypoint: "/docker-entrypoint.sh"
branding:
  icon: "check-circle"
  color: "green"
